web: gunicorn app:app 



# Make sure you’re in your project folder
cd "C:\Users\LAPTOP WORLD\Desktop\perfueme"

# Add ALL files (updated, new, deleted)
git add .

# Commit with a clear message
git commit -m "Full replace with latest code"

# Force push to overwrite GitHub main branch
git push -f origin main


